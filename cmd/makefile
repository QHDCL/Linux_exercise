#makefile注释以#符号开头
#编写规则:
#	目标对象名称:依赖对象名称
#   
#  make程序解释Makefile文件中规则流程:
#  	1.先找目标对象(永远只生成第一个对象,第一个对象命令执行完毕之后就退出了)
#  	2.找到目标对象后,与依赖对象进行时间属性对比
#  		判断程序是否是最新的,是否需要重新编译
#  	3.执行为了生成目标对象所要执行的命令
#  		这条命令,不一定要生成目标对象
#    *	make	这条命令可以直接在外部指定要执行生成的对象
# 		make clean
# 	伪对象:不管目标对象是不是最新的,都需要 重新生成
# 		声明:   .PHONY

#	定义all对象是因为make只找第一个对象生成,其他对象将不再执行,但是又想生成多个对象,那么就需要定义第一个对象他的依赖对象是我们想要生成的其他所有对象
#	make为了生成第一个对象,会间接的去生成这些依赖对象
#
#	makefile中的预定义变量
#	用于执行命令中的
#	$@:    指的是目标对象
#	$^:	   指的是所有的依赖对象
#	$<:   指的是 所有依赖对象中的 第一个依赖对象
#	wildcard 关键字,用于获取文件名
#	$()用于使用一个变量


src=$(wildcard ./*.c)

all:main test

main:$(src)
	gcc -g $^ -o $@
test:main.c child.c
	gcc main.c child.c -o test
.PHONY:clean
clean: 
	rm main
	rm test
